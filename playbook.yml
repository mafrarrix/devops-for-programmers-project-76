- name: Main playbook
  hosts: all
  become: yes
  roles:
    - role: geerlingguy.pip
    - role: geerlingguy.docker

  #inserire task per la copia dei file dockercompose sulla macchina di destinazione

  tasks:
  #inserire task per la copia dei file dockercompose sulla macchina di destinazione
  - name: Deploy Redmine
  #modificar il comando per utilizzare un file docker compose e carica le varibili da un env file
    ansible.builtin.command: docker run -d -p 3000:3000  --name some-redmine redmine
    tags: 
      - deploy



       
